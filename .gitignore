# 데이터셋·모델 파일은 절대 커밋 금지 (라이선스: NOMO 재배포 금지, Texel CC BY-NC, SMPL 재배포 불가)
data/external/
models/
reports/
.venv/
__pycache__/
*.pyc
.pytest_cache/
external/
data/generated/

# 착의 스캔 워크스트림 — LICENSE-G0 참조.
# 학습 가중치는 CAPE/SIZER 파생물로 취급: 사용 범위 서면 확인 전까지 배포 금지.
checkpoints/
*.onnx
*.pt
*.pth
*.ckpt
# 라이선스 원문·라이선서 회신은 보관하되 커밋 금지.
# 우리가 작성한 정책 문서는 버전 관리 대상 (정책은 이력이 남아야 함).
docs/licenses/*
!docs/licenses/README.md
!docs/licenses/scope.md
!docs/licenses/public-material.md
!docs/licenses/ethics.md
!docs/licenses/provenance.md

# Dataset archives — licensed material, never committed regardless of where
# they are unpacked or staged (CAPE, SIZER, and anything else that arrives
# as a zip). See docs/licenses/.
*.zip
*.7z
*.tar.gz
cape_release/
